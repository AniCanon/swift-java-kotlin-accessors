group = "dev.anicanon.swiftjava"
version = "0.1.1"

allprojects {
    group = rootProject.group
    version = rootProject.version
    repositories {
        mavenCentral()
        google()
        gradlePluginPortal()
    }
}
